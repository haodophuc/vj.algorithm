package vj.algorithm.featureset;
/*
 * 1. Save WeakClassifier txt cho mỗi ảnh train (19x19 -pos, neg, label 1 hoặc -1) (thresold, Feature[] (type, x,y,w,h)
 * mỗi Feature có fValue, so sánh với thresold --> 1 hoặc -1)
 * 2. Tính toán yj theo f của Feature so khớp threhold
 */
public class WeakClassifier {

}
